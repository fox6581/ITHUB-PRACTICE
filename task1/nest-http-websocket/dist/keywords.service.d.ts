export declare class KeywordsService {
    private readonly data;
    private workerPool;
    handleWebSocketConnection(socket: any, req: any): Promise<void>;
    private processMessage;
    private downloadContent;
}
